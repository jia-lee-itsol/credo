// This is a basic Flutter widget test.

import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // TODO: 앱 통합 테스트 구현
    // CredoApp은 ProviderScope와 Router가 필요하므로
    // 개별 위젯 테스트를 작성하는 것이 권장됩니다.
    expect(true, isTrue);
  });
}
