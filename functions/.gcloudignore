# This file specifies files that are *not* uploaded to Google Cloud
# using gcloud. It follows the same syntax as .gitignore

# Compiled JavaScript files
lib/**/*.js
lib/**/*.js.map

# TypeScript v1 declaration files
typings/

# Node.js dependency directory
node_modules/
*.local

# Environment variables
.env
.env.local

# Service account key files should NOT be ignored for deployment
# (needed for FCM authentication)
# serviceAccountKey.json is explicitly included

# Other JSON files (but allow serviceAccountKey.json)
*.json
!package.json
!package-lock.json
!tsconfig.json
!serviceAccountKey.json

